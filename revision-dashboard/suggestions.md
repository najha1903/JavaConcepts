# Suggested additions

Generated by `npm run revise`. Nothing here is written to your notes until you accept it.

A suggestion is raised only when this can point at the exact API or the exact construct, and only for a chapter that is finished.

## Chapter 7: Switch Statements

**Print Number In A World Coding Challenge** — a switch

_the code uses a switch, and the notes never explain it_

```java
// a switch :- the code in this topic uses this, but the notes do not explain it.
// Note :- a reader who has not met a switch yet cannot follow the example.
```

## Chapter 8: Java Looping Concepts

**Pattern Printer Deep Problem** — a for loop

_the code uses a for loop, and the notes never explain it_

```java
// a for loop :- the code in this topic uses this, but the notes do not explain it.
// Note :- a reader who has not met a for loop yet cannot follow the example.
```

## Chapter 9: While And Do While Looping Concepts

**Digit Sum Coding Challenge** — a while loop

_the code uses a while loop, and the notes never explain it_

```java
// a while loop :- the code in this topic uses this, but the notes do not explain it.
// Note :- a reader who has not met a while loop yet cannot follow the example.
```

## Chapter 9: While And Do While Looping Concepts

**Even Digit Sum Coding Challenge** — a while loop

_the code uses a while loop, and the notes never explain it_

```java
// a while loop :- the code in this topic uses this, but the notes do not explain it.
// Note :- a reader who has not met a while loop yet cannot follow the example.
```

## Chapter 9: While And Do While Looping Concepts

**Floor Pack Problem Coding Challenge** — a while loop

_the code uses a while loop, and the notes never explain it_

```java
// a while loop :- the code in this topic uses this, but the notes do not explain it.
// Note :- a reader who has not met a while loop yet cannot follow the example.
```

## Chapter 9: While And Do While Looping Concepts

**Greatest Common Divisor Coding Challenge** — Math.max

_the code calls Math.max, and the notes never mention max_

```java
// Math.max :- used in this topic's code but not explained in the notes.
// Note :- say what max does, what it takes, and what it gives back, so the code above is not taken on trust.
```

## Chapter 9: While And Do While Looping Concepts

**Greatest Common Divisor Coding Challenge** — Math.min

_the code calls Math.min, and the notes never mention min_

```java
// Math.min :- used in this topic's code but not explained in the notes.
// Note :- say what min does, what it takes, and what it gives back, so the code above is not taken on trust.
```

## Chapter 9: While And Do While Looping Concepts

**Largest Prime Factor Coding Challenge** — a while loop

_the code uses a while loop, and the notes never explain it_

```java
// a while loop :- the code in this topic uses this, but the notes do not explain it.
// Note :- a reader who has not met a while loop yet cannot follow the example.
```

## Chapter 9: While And Do While Looping Concepts

**Last Digit Checker Coding Challenge** — a while loop

_the code uses a while loop, and the notes never explain it_

```java
// a while loop :- the code in this topic uses this, but the notes do not explain it.
// Note :- a reader who has not met a while loop yet cannot follow the example.
```

## Chapter 9: While And Do While Looping Concepts

**Shared Digit Coding Challenge** — a while loop

_the code uses a while loop, and the notes never explain it_

```java
// a while loop :- the code in this topic uses this, but the notes do not explain it.
// Note :- a reader who has not met a while loop yet cannot follow the example.
```

## Chapter 9: While And Do While Looping Concepts

**Number Guesser Deep Problem** — a while loop

_the code uses a while loop, and the notes never explain it_

```java
// a while loop :- the code in this topic uses this, but the notes do not explain it.
// Note :- a reader who has not met a while loop yet cannot follow the example.
```

## Chapter 11: Parsing Values Reading Input

**Input Calculator Challenge** — Integer.parseInt

_the code calls Integer.parseInt, and the notes never mention parseInt_

```java
// Integer.parseInt :- used in this topic's code but not explained in the notes.
// Note :- say what parseInt does, what it takes, and what it gives back, so the code above is not taken on trust.
```

## Chapter 11: Parsing Values Reading Input

**Input Calculator Challenge** — Scanner.nextLine

_the code calls Scanner.nextLine, and the notes never mention nextLine_

```java
// Scanner.nextLine :- used in this topic's code but not explained in the notes.
// Note :- say what nextLine does, what it takes, and what it gives back, so the code above is not taken on trust.
```

## Chapter 11: Parsing Values Reading Input

**Min Max Coding Challenge** — Integer.parseInt

_the code calls Integer.parseInt, and the notes never mention parseInt_

```java
// Integer.parseInt :- used in this topic's code but not explained in the notes.
// Note :- say what parseInt does, what it takes, and what it gives back, so the code above is not taken on trust.
```

## Chapter 11: Parsing Values Reading Input

**Min Max Coding Challenge** — Scanner

_the code calls Scanner, and the notes never mention Scanner_

```java
// Scanner :- used in this topic's code but not explained in the notes.
// Note :- say what Scanner does, what it takes, and what it gives back, so the code above is not taken on trust.
```

## Chapter 11: Parsing Values Reading Input

**Min Max Coding Challenge** — Scanner.nextLine

_the code calls Scanner.nextLine, and the notes never mention nextLine_

```java
// Scanner.nextLine :- used in this topic's code but not explained in the notes.
// Note :- say what nextLine does, what it takes, and what it gives back, so the code above is not taken on trust.
```

## Chapter 11: Parsing Values Reading Input

**Reading User Input Challenge** — Integer.parseInt

_the code calls Integer.parseInt, and the notes never mention parseInt_

```java
// Integer.parseInt :- used in this topic's code but not explained in the notes.
// Note :- say what parseInt does, what it takes, and what it gives back, so the code above is not taken on trust.
```

## Chapter 11: Parsing Values Reading Input

**Reading User Input Challenge** — Scanner.nextLine

_the code calls Scanner.nextLine, and the notes never mention nextLine_

```java
// Scanner.nextLine :- used in this topic's code but not explained in the notes.
// Note :- say what nextLine does, what it takes, and what it gives back, so the code above is not taken on trust.
```

## Chapter 11: Parsing Values Reading Input

**Input Validator Deep Problem** — Integer.parseInt

_the code calls Integer.parseInt, and the notes never mention parseInt_

```java
// Integer.parseInt :- used in this topic's code but not explained in the notes.
// Note :- say what parseInt does, what it takes, and what it gives back, so the code above is not taken on trust.
```

## Chapter 11: Parsing Values Reading Input

**Input Validator Deep Problem** — Double.parseDouble

_the code calls Double.parseDouble, and the notes never mention parseDouble_

```java
// Double.parseDouble :- used in this topic's code but not explained in the notes.
// Note :- say what parseDouble does, what it takes, and what it gives back, so the code above is not taken on trust.
```

## Chapter 11: Parsing Values Reading Input

**Input Validator Deep Problem** — StringBuilder.append

_the code calls StringBuilder.append, and the notes never mention append_

```java
// StringBuilder.append :- used in this topic's code but not explained in the notes.
// Note :- say what append does, what it takes, and what it gives back, so the code above is not taken on trust.
```

## Chapter 11: Parsing Values Reading Input

**Input Validator Deep Problem** — StringBuilder

_the code calls StringBuilder, and the notes never mention StringBuilder_

```java
// StringBuilder :- used in this topic's code but not explained in the notes.
// Note :- say what StringBuilder does, what it takes, and what it gives back, so the code above is not taken on trust.
```

## Chapter 12: Exception Handling

**Safe Division Challenge** — try / catch

_the code uses try / catch, and the notes never explain it_

```java
// try / catch :- the code in this topic uses this, but the notes do not explain it.
// Note :- a reader who has not met try / catch yet cannot follow the example.
```

## Chapter 12: Exception Handling

**Robust Calculator Deep Problem** — try / catch

_the code uses try / catch, and the notes never explain it_

```java
// try / catch :- the code in this topic uses this, but the notes do not explain it.
// Note :- a reader who has not met try / catch yet cannot follow the example.
```

## Chapter 12: Exception Handling

**Robust Calculator Deep Problem** — throwing an exception

_the code uses throwing an exception, and the notes never explain it_

```java
// throwing an exception :- the code in this topic uses this, but the notes do not explain it.
// Note :- a reader who has not met throwing an exception yet cannot follow the example.
```

## Chapter 13: OOP Concepts

**Method Over Loading Challenge** — a static member

_the code uses a static member, and the notes never explain it_

```java
// a static member :- the code in this topic uses this, but the notes do not explain it.
// Note :- a reader who has not met a static member yet cannot follow the example.
```

## Chapter 13: OOP Concepts

**Super Keyword Use Example** — inheritance

_the code uses inheritance, and the notes never explain it_

```java
// inheritance :- the code in this topic uses this, but the notes do not explain it.
// Note :- a reader who has not met inheritance yet cannot follow the example.
```

## Chapter 13: OOP Concepts

**Main** — a static member

_the code uses a static member, and the notes never explain it_

```java
// a static member :- the code in this topic uses this, but the notes do not explain it.
// Note :- a reader who has not met a static member yet cannot follow the example.
```

## Chapter 13: OOP Concepts

**Fish** — inheritance

_the code uses inheritance, and the notes never explain it_

```java
// inheritance :- the code in this topic uses this, but the notes do not explain it.
// Note :- a reader who has not met inheritance yet cannot follow the example.
```

## Chapter 13: OOP Concepts

**Inheritance** — a static member

_the code uses a static member, and the notes never explain it_

```java
// a static member :- the code in this topic uses this, but the notes do not explain it.
// Note :- a reader who has not met a static member yet cannot follow the example.
```

## Chapter 13: OOP Concepts

**Employee** — Object.toString

_the code calls Object.toString, and the notes never mention toString_

```java
// Object.toString :- used in this topic's code but not explained in the notes.
// Note :- say what toString does, what it takes, and what it gives back, so the code above is not taken on trust.
```

## Chapter 13: OOP Concepts

**Worker** — Object.toString

_the code calls Object.toString, and the notes never mention toString_

```java
// Object.toString :- used in this topic's code but not explained in the notes.
// Note :- say what toString does, what it takes, and what it gives back, so the code above is not taken on trust.
```

## Chapter 13: OOP Concepts

**Method Overloading In Java** — a static member

_the code uses a static member, and the notes never explain it_

```java
// a static member :- the code in this topic uses this, but the notes do not explain it.
// Note :- a reader who has not met a static member yet cannot follow the example.
```

## Chapter 13: OOP Concepts

**Dog** — inheritance

_the code uses inheritance, and the notes never explain it_

```java
// inheritance :- the code in this topic uses this, but the notes do not explain it.
// Note :- a reader who has not met inheritance yet cannot follow the example.
```

## Chapter 13: OOP Concepts

**Shape Hierarchy Deep Problem** — inheritance

_the code uses inheritance, and the notes never explain it_

```java
// inheritance :- the code in this topic uses this, but the notes do not explain it.
// Note :- a reader who has not met inheritance yet cannot follow the example.
```

## Chapter 13: OOP Concepts

**Shape Hierarchy Deep Problem** — a static member

_the code uses a static member, and the notes never explain it_

```java
// a static member :- the code in this topic uses this, but the notes do not explain it.
// Note :- a reader who has not met a static member yet cannot follow the example.
```

## Chapter 14: Strings

**String Method And Best Practices** — printf

_the code calls printf, and the notes never mention printf_

```java
// printf :- used in this topic's code but not explained in the notes.
// Note :- say what printf does, what it takes, and what it gives back, so the code above is not taken on trust.
```

## Chapter 14: Strings

**String Method And Best Practices** — String formatting

_the code uses String formatting, and the notes never explain it_

```java
// String formatting :- the code in this topic uses this, but the notes do not explain it.
// Note :- a reader who has not met String formatting yet cannot follow the example.
```

## Chapter 14: Strings

**String Formatting Challenge** — an array

_the code uses an array, and the notes never explain it_

```java
// an array :- the code in this topic uses this, but the notes do not explain it.
// Note :- a reader who has not met an array yet cannot follow the example.
```

## Chapter 14: Strings

**String Analyzer Deep Problem** — String.format

_the code calls String.format, and the notes never mention format_

```java
// String.format :- used in this topic's code but not explained in the notes.
// Note :- say what format does, what it takes, and what it gives back, so the code above is not taken on trust.
```

## Chapter 14: Strings

**String Analyzer Deep Problem** — String.charAt

_the code calls String.charAt, and the notes never mention charAt_

```java
// String.charAt :- used in this topic's code but not explained in the notes.
// Note :- say what charAt does, what it takes, and what it gives back, so the code above is not taken on trust.
```

## Chapter 14: Strings

**String Analyzer Deep Problem** — String.toCharArray

_the code calls String.toCharArray, and the notes never mention toCharArray_

```java
// String.toCharArray :- used in this topic's code but not explained in the notes.
// Note :- say what toCharArray does, what it takes, and what it gives back, so the code above is not taken on trust.
```

## Chapter 14: Strings

**String Analyzer Deep Problem** — String.toLowerCase

_the code calls String.toLowerCase, and the notes never mention toLowerCase_

```java
// String.toLowerCase :- used in this topic's code but not explained in the notes.
// Note :- say what toLowerCase does, what it takes, and what it gives back, so the code above is not taken on trust.
```

## Chapter 14: Strings

**String Analyzer Deep Problem** — String.trim

_the code calls String.trim, and the notes never mention trim_

```java
// String.trim :- used in this topic's code but not explained in the notes.
// Note :- say what trim does, what it takes, and what it gives back, so the code above is not taken on trust.
```

## Chapter 14: Strings

**String Analyzer Deep Problem** — String.split

_the code calls String.split, and the notes never mention split_

```java
// String.split :- used in this topic's code but not explained in the notes.
// Note :- say what split does, what it takes, and what it gives back, so the code above is not taken on trust.
```

## Chapter 14: Strings

**String Analyzer Deep Problem** — String.replace

_the code calls String.replace, and the notes never mention replace_

```java
// String.replace :- used in this topic's code but not explained in the notes.
// Note :- say what replace does, what it takes, and what it gives back, so the code above is not taken on trust.
```

## Chapter 14: Strings

**String Analyzer Deep Problem** — StringBuilder.append

_the code calls StringBuilder.append, and the notes never mention append_

```java
// StringBuilder.append :- used in this topic's code but not explained in the notes.
// Note :- say what append does, what it takes, and what it gives back, so the code above is not taken on trust.
```

## Chapter 14: Strings

**String Analyzer Deep Problem** — StringBuilder

_the code calls StringBuilder, and the notes never mention StringBuilder_

```java
// StringBuilder :- used in this topic's code but not explained in the notes.
// Note :- say what StringBuilder does, what it takes, and what it gives back, so the code above is not taken on trust.
```

## Chapter 14: Strings

**String Analyzer Deep Problem** — an array

_the code uses an array, and the notes never explain it_

```java
// an array :- the code in this topic uses this, but the notes do not explain it.
// Note :- a reader who has not met an array yet cannot follow the example.
```
